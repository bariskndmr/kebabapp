import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 10,
    },
    label : {
        fontWeight : 'bold',
        fontSize : 18,
    },
    input: {
        borderWidth : 1,
        borderColor: 'gray',
        padding: 10,
        margin : 5,
        borderRadius : 5,
    }
});